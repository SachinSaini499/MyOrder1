<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MyOrder.ViewModel"
             x:Class="MyOrder.LoginPage" BackgroundColor="FloralWhite" >
    <ContentPage.BindingContext>
        <viewmodels:LoginViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <Frame Padding="0,0,0,0"  VerticalOptions="CenterAndExpand"  OutlineColor="White" HasShadow="True" BackgroundColor="#171717">

            <Grid Margin="20" BackgroundColor="FloralWhite" ColumnSpacing="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" Grid.Column="0" TextColor="#171717" Grid.ColumnSpan="2" x:Name="lblHeading" Text="Welcome to  Myorder" HorizontalOptions="Center" />
                <Label Grid.Row="1" TextColor="#171717" Grid.Column="0" Text="EmailId" x:Name="lblEmailId" />
                <Entry Grid.Row="1" TextColor="#171717"  Grid.Column="1" x:Name="entryEmail"></Entry>
                <Label Grid.Row="2" TextColor="#171717" Grid.Column="0" x:Name="lblPassword" Text="Password"  />
                <Entry Grid.Row="2"  TextColor="#171717" Grid.Column="1" x:Name="entryPassword"></Entry>
                <Button Grid.Row="3" Grid.Column="0" BackgroundColor="#343434" Grid.ColumnSpan="2" HorizontalOptions="Center" x:Name="btnLogin" Text="Login" Command="{Binding LoginCommand}"></Button>
            </Grid>
        </Frame>
    </ContentPage.Content>
</ContentPage>