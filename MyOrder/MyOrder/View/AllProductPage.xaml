<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:viewmodels="clr-namespace:MyOrder.ViewModel"
             x:Class="MyOrder.View.AllProductPage">
    <ContentPage.BindingContext>
        <viewmodels:AllProductModel/>
    </ContentPage.BindingContext>
    <StackLayout>
        <Grid Margin="20" VerticalOptions="CenterAndExpand" Padding="10"  >
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="2" HorizontalOptions="StartAndExpand" Grid.Row="0" Text="Add Product" Command="{Binding AddProductCommand}" ></Button>
        </Grid>
    </StackLayout>
  
</ContentPage>